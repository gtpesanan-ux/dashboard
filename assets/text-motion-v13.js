
(()=>{const reduce=window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches;const nodes=[...document.querySelectorAll('main .kicker, main h1, main section h2')];nodes.forEach(el=>{el.classList.add('dig-motion-text');if(el.matches('.kicker'))el.classList.add('dig-motion-kicker');else if(el.tagName==='H1')el.classList.add('dig-motion-h1');else el.classList.add('dig-motion-h2')});if(reduce||!('IntersectionObserver'in window)){nodes.forEach(el=>el.classList.add('is-visible'));return;}const io=new IntersectionObserver(entries=>{for(const e of entries){if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}},{threshold:.12,rootMargin:'0px 0px -6% 0px'});nodes.forEach(el=>io.observe(el));})();
